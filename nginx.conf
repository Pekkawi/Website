# Rate limit zone (optional)
limit_req_zone $binary_remote_addr zone=mylimit:10m rate=10r/s;

# Redirect HTTP -> HTTPS
server {
    listen 80;
    server_name localhost;
    return 301 https://$host$request_uri;
}

# HTTPS with self-signed cert
server {
    listen 443 ssl;
    server_name localhost;

    ssl_certificate     /etc/nginx/certs/selfsigned.crt;
    ssl_certificate_key /etc/nginx/certs/selfsigned.key;

    # Allow WebSocket upgrades
    location /socket.io/ {
        proxy_pass http://host.docker.internal:3000; # or 127.0.0.1:3000 if not in docker
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
        proxy_buffering off;
        proxy_set_header X-Accel-Buffering no;
    }

    # Normal Next.js traffic
    location / {
        proxy_pass http://host.docker.internal:3000; # or 127.0.0.1:3000
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}


 limit_req_zone \$binary_remote_addr zone=mylimit:10m rate=10r/s;

# server {
#     listen 80;
#     server_name $DOMAIN_NAME;

#     # Redirect all HTTP requests to HTTPS
#     return 301 https://\$host\$request_uri;
# }

# upstream socket_nodes {
#     ip_hash;
#     server thecoreworkshop.com
# }

# server {
#     listen 443 ssl;
#     server_name $DOMAIN_NAME;

#     # ssl_certificate /etc/letsencrypt/live/$DOMAIN_NAME/fullchain.pem;
#     # ssl_certificate_key /etc/letsencrypt/live/$DOMAIN_NAME/privkey.pem;
#     # include /etc/letsencrypt/options-ssl-nginx.conf;
#     # ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

#     # Enable rate limiting
#     limit_req zone=mylimit burst=20 nodelay;

#     location /socket.io {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade \$http_upgrade;
#         proxy_set_header Connection 'upgrade'
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Host \$host;
#         proxy_cache_bypass \$http_upgrade;
#         # Disable buffering for streaming support
#         proxy_buffering off;
#         proxy_set_header X-Accel-Buffering no;
#     }
# }

# events {
#     worker_connections  1024;
# }

# http {
#     server {
#         listen 80;
#         server_name workshopthecore.com www.workshopthecore.com;

#         location / {
#             proxy_pass http://nextjs:3000;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }
#     }
# }