# LOCAL BUILD


events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name localhost;

        location / {
            proxy_pass http://nextjs:3000;
            proxy_set_header HOST $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme
            
        }
    }
}

# Development build

# events {
#     worker_connections 1024;
# }

# http {
#     # Rate limiting zone (activate with "limit_req" inside locations if you want)
#     limit_req_zone $binary_remote_addr zone=mylimit:10m rate=10r/s;
        
#     upstream nextjs {
#         # Docker service name "nextjs" on port 3000
#         server nextjs:3000;
#     }
        
#         # HTTP -> HTTPS redirect
#     server {
#         listen 80 default_server;
#         server_name _;
#         return 301 https://$host$request_uri;
#     }
        
#         # HTTPS server
#     server {
#         listen 443 ssl http2 default_server;       
#         server_name _;
            
#         ssl_certificate /etc/nginx/ssl/certs/cert.crt;
#         ssl_certificate_key /etc/nginx/ssl/certs/cert.key;
#         ssl_protocols TLSv1.2 TLSv1.3;
#         ssl_prefer_server_ciphers on;
            
#             # WebSocket support
#         location /socket.io/ {
#             proxy_pass http://nextjs;
#             proxy_http_version 1.1;
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection "upgrade";
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#             proxy_read_timeout 75s;
#             proxy_send_timeout 75s;
#             proxy_cache_bypass $http_upgrade;
#             proxy_buffering off;
#         }
            
#             # Normal traffic
#         location / {
#             proxy_pass http://nextjs;
#             proxy_http_version 1.1;
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection 'upgrade';
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#             proxy_cache_bypass $http_upgrade;
#         }
#     }
# }