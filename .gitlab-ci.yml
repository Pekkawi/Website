image: docker

services:
  - docker:dind

stages:
  - deploy

deploy-prod:
  stage: deploy
  script:
    - docker build . -t my-nextjs-app:latest
    - docker run -d -p 3000:3000 --rm --name my-nextjs-app my-nextjs-app:latest
  only:
    - main