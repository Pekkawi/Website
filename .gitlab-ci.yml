image: docker

services:
  - docker:dind

stages:
  - deploy

deploy-prod:
  stage: deploy
  script:
    - docker build . -t thecore-docker:latest # Updated image name
    - docker run -d -p 3000:3000 --rm --name thecore-docker:latest thecore-docker # Updated image name
  only:
    - main
